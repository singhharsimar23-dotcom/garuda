{
  "buildCommand": "cd frontend && npm install && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py?__url__=/$1"
    }
  ],
  "crons": [
    {
      "path": "/api/collect",
      "schedule": "0 */4 * * *"
    }
  ]
}
