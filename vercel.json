{
  "buildCommand": "cd frontend && npm install && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ],
  "crons": [
    {
      "path": "/api/collect",
      "schedule": "0 6 * * *"
    }
  ]
}
